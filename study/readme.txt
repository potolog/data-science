1. c:\data_sc 디렉토리를 생성한다.
2. blog.naver.com/vega2k 블로그에서
  Data Science 입문 글에 있는 첨부파일을 다운로드 받아서 c:\data_sc 에 저장한다.
3. cmd 창에서 설치된 python의 버전을 확인한다.
  >python --version
  Python 3.6.5 :: Anaconda, Inc.

  python library가 설치된 디렉토리를 확인한다.
  >pip show seaborn
  Location: c:\users\kosta\anaconda3

 miniconda 설치된 경우
  > pip install numpy
   : 벡터, 행렬 연산 , Array 처리, 수학함수
  > pip install pandas
   : table 형태로 데이터를 핸들링 , SQL 로 할수 기능들을 대신
  > pip install jupyter
   : jupyter notebook 은 editing tool
     (ipython을 Browser 상에서 사용할 수 있도록 해준것)
  > pip install matplotlib
  > pip install seaborn
  : 시각화(Visualization), graph
  > pip install scikit-learn
  : machine learning(ML)

4. 3번의 명령이 실행되지 않으면 anaconda3를 설치해야 합니다.
  설치 파일은 usb로 배포합니다.

  웹상에서 다운로드 받으려면
  https://www.anaconda.com/distribution/

  pycharm(jetbrains), MS visual studio code (plugin 설치 후 가능)

  ipython
  :

5. data-1.zip, data-2.zip 파일의 압축을 풀어서 data/ 폴더에 저장한다.



  python.org
  anaconda
  miniconda

